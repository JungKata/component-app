import { Component, ReactNode } from "react";

interface Cards{
    caption: string;
    description: string;
    image: string;

}

export default class Card extends Component{
    render(): ReactNode {
        return <div>
           
        </div>
    }
}